Imports System.IO
Module Module1
    'Public Sub ExitApplication(currentForm As Form)
    '    Dim result As DialogResult = MessageBox.Show("Do you want to exit?", "Exit Confirmation", MessageBoxButtons.YesNo, MessageBoxIcon.Question)

    '    If result = DialogResult.Yes Then
    '        ' Hide the current form
    '        currentForm.Hide()

    '        ' Show Form1 from userForm project
    '        Dim userForm1 As New userForm.Form1()
    '        userForm1.Show()
    '    End If
    'End Sub
End Module
